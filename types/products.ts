export type TProduct = {
  title: string;
  description: string;
  id: string;
};

export type TProductWithAmount = {
  id: number;
  name: string;
  amount: number;
  description: string;
  image: string;
  category: string;
};
